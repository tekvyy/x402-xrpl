node_modules
dist
*.tsbuildinfo
.env
.env.local
.env.mainnet
.DS_Store

# Can hold an npm publish token (//registry.npmjs.org/:_authToken=...).
.npmrc

# pnpm copies the root LICENSE into each package at pack time; the root file is
# the only source of truth, so the copies are build artifacts.
packages/*/LICENSE

.claude
